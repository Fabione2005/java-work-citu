/* Author: JP19207 */
SET serveroutput ON
SET DEFINE OFF
SET ECHO ON
ALTER SESSION SET current_schema=OBTAPP;

ALTER TABLE OBTAPP.AMT_CLIENT_DEFINITION_ACTION DROP (
    REQUEST_ID,
    IS_SEPA_ELIGIBLE
);
