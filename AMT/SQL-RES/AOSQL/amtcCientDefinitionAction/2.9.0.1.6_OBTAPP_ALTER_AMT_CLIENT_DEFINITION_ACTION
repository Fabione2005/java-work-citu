/* Author: JP19207 */
SET serveroutput ON
SET DEFINE OFF
SET ECHO ON
ALTER SESSION SET current_schema=OBTAPP;

ALTER TABLE OBTAPP.AMT_CLIENT_DEFINITION_ACTION ADD (
    REQUEST_ID VARCHAR2(50 CHAR),
    IS_SEPA_ELIGIBLE VARCHAR2(1 CHAR)
);
