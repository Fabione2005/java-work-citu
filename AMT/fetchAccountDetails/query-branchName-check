SELECT
    A.BRANCH_CODE,
    RE.STR1 AS BRANCH_NAME,
    RE.STR2 AS BRANCH_CODE_FROM_LOOKUP,
    RE.STATUS
FROM AMT_ACCOUNT_REQUEST A
LEFT JOIN VW_AMT_REFS_BRANCHCODESLOOKUP RE
       ON RE.STR2 = A.BRANCH_CODE
      AND RE.STATUS = 1
WHERE A.BRANCH_CODE IS NOT NULL
FETCH FIRST 50 ROWS ONLY;
